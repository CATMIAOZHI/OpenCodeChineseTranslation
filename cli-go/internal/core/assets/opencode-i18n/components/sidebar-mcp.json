{
  "file": "packages/tui/src/feature-plugins/sidebar/mcp.tsx",
  "description": "侧边栏 MCP 状态",
  "replacements": {
    ">Connected</Match>": ">已连接</Match>",
    ">Disabled</Match>": ">已禁用</Match>",
    ">Needs auth</Match>": ">需要认证</Match>",
    ">Needs client ID</Match>": ">需要客户端 ID</Match>",
    "({on()} active{bad() > 0 ? `, ${bad()} error${bad() > 1 ? \"s\" : \"\"}` : \"\"})": "({on()} 个活跃{bad() > 0 ? `, ${bad()} 个错误` : \"\"})"
  }
}